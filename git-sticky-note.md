# See what changed
git status

# Stage everything you've changed
git add .

# Save a snapshot
git commit -m "Describe what you did"

# Copy the snapshot to GitHub
git push

# See your history
git log --oneline

# See what changed
git diff

# Tag the version from time to time
git tag v1.0

# Push tags 
git push origin v1.0

# See tags
git tag